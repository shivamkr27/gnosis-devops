.terraform/
*.tfstate
*.tfstate.backup
.terraform.lock.hcl
*.tfvars
.env
.env.prod
*.pem
*.key
__pycache__/
.streamlit/
node_modules/

# secrets.yaml is tracked in git as a template (placeholder values only).
# ArgoCD ignoreDifferences is configured to never overwrite live secret data.
# To apply real secrets: kubectl create secret generic gnosis-secrets ... -n gnosis
