apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: gnosis
  labels:
    app: api-gateway
    project: gnosis
    monitoring: enabled
spec:
  type: ClusterIP
  selector:
    app: api-gateway
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
