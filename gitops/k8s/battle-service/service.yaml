apiVersion: v1
kind: Service
metadata:
  name: battle-service
  namespace: gnosis
  labels:
    app: battle-service
spec:
  type: ClusterIP
  selector:
    app: battle-service
  ports:
    - port: 3005
      targetPort: 3005
      protocol: TCP
